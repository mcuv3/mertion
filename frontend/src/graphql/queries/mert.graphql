query Mert($mertId: String) {
  mert(mertId: $mertId) {
    ...BaseMert
    father {
      id
      createdAt
      likes
      dislikes
      user {
        id
        username
        picture
      }
    }
  }
}
