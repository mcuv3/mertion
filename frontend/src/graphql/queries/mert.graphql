query Mert($mertId: String!) {
  mert(mertId: $mertId) {
    id
    mert
    createdAt
    picture
    likes
    dislikes
    father {
      id
      createdAt
      likes
      dislikes
      user {
        username
        picture
      }
    }
    user {
      username
      picture
    }
  }
}
