query UserReactions($reaction: Reactions!, $mertId: String!) {
  usersReactions(reaction: $reaction, mertId: $mertId) {
    success
    message
    users {
      id
      username
      picture
    }
  }
}
